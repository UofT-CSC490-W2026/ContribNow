# ContribNow Desktop App Configuration

# Hosted API base URL
API_URL=https://l6a25jv8zi.execute-api.ca-central-1.amazonaws.com

# Maximum file size (bytes) before truncating selected file contents
MAX_FILE_SIZE=100000

# Timeout (seconds) for the generate-onboarding API call
API_TIMEOUT=120

# AWS Bedrock Agent (for /ask)
BEDROCK_AGENT_ID=
BEDROCK_AGENT_ALIAS_ID=
AWS_REGION=ca-central-1
